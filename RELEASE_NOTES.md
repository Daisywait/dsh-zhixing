# v0.3.4

- 学习面板增加可展开的全部主题列表，主题多时不再依赖隐藏下拉框。
- 修正继续学习的会话打开顺序，先定位会话再切换视图，确保请求草稿可见。

# v0.3.3

- 在 dsh 侧栏底部增加“知行学习”入口，欢迎页也可打开学习面板。
- 入口共用会话桥接，不改变草稿或发送行为；原学习标签保留。

# v0.3.2

- 根据全书通读修正诊断流程，下一步可携带缺口、依据和材料选择，引用已有作答并保存变更历史。
- 已有例子直接展示，支持切换内容和案例，按具体情境、推测过程、结果、规则组织。
- 新增局部求助和材料质疑入口，请求带上当前材料，不写成错误作答。
- 兼容无诊断字段的旧档案；不自动重写个人历史，不改变会话发送方式。
- 以桌面及桌面嵌入界面为验证范围。自动检查不证明 AI 教学效果。

# v0.2.2

首个面向分发的知行 dsh 插件。

- 在 dsh 对话中增加“学习”标签，保留原有聊天输入框。
- 首页以学习问题和下一步为中心，靶图与证据按需展开。
- 学习请求填入当前草稿，保留已有内容，由用户发送。
- 内置 zhixing-learning 技能与 zhixing_archive 工具，无运行时 npm 依赖。
- 个人档案独立保存，支持导出与恢复到空白档案，示例与真实证据隔离。
- 同源面板路由复用 dsh 登录认证，并限制本机访问。

验证：7 项自动测试通过；在 dsh 0.1.2-rc.1 的新会话中成功加载技能与读取档案；桌面和手机尺寸检查完成；确认未认证访问返回401。

已知限制：实际系统验证为 Windows。其他系统的测试工作流已配置，运行结果需在 GitHub 推送后确认。正在进行的题目仍在对话内展示，学习页主要显示下一步和已记录的理解。恢复档案不支持覆盖或自动合并已有主题。
# v0.3.0

- 新学习主题默认创建独立 dsh 会话，也可选择已有会话。
- 主题可关联多个会话，支持跳转、设为主要会话、解除关联。
- 继续练习进入主要会话草稿，保留原草稿，不自动发送。
- 关联随档案迁移；不可用的会话提示重新关联，不影响学习证据。
- 关联不读取聊天内容，也不自动合并学习记录。
- 新增会话关联、认证及前端桥接测试。
# v0.3.1

- 依据《学习观》第18、27、30–34章校正学习流程，取消固定层级闯关。
- 完全陌生的主题先获得讲解与对齐实例，不强迫初学者先举例或回答输入输出。
- 区分教学例子与验证题；只有验证题等待回答后揭示答案。
- 首页支持继续学习、先讲解、换例子、主动验证，已记录材料可展开回看。
- 继续旧主题时重新评估原定下一步；不覆盖档案或学习证据。
- 新增浏览器交互验收，方法页记录来源与产品约定；实际模型教学效果仍需真实对话检验。
