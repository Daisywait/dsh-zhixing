# v0.2.2

首个面向分发的知行 dsh 插件。

- 在 dsh 对话中增加“学习”标签，保留原有聊天输入框。
- 首页以学习问题和下一步为中心，靶图与证据按需展开。
- 学习请求填入当前草稿，保留已有内容，由用户发送。
- 内置 zhixing-learning 技能与 zhixing_archive 工具，无运行时 npm 依赖。
- 个人档案独立保存，支持导出与恢复到空白档案，示例与真实证据隔离。
- 同源面板路由复用 dsh 登录认证，并限制本机访问。

验证：7 项自动测试通过；在 dsh 0.1.2-rc.1 的新会话中成功加载技能与读取档案；桌面和手机尺寸检查完成；确认未认证访问返回401。

已知限制：实际系统验证为 Windows。其他系统的测试工作流已配置，运行结果需在 GitHub 推送后确认。正在进行的题目仍在对话内展示，学习页主要显示下一步和已记录的理解。恢复档案不支持覆盖或自动合并已有主题。
# v0.3.0

- 新学习主题默认创建独立 dsh 会话，也可选择已有会话。
- 主题可关联多个会话，支持跳转、设为主要会话、解除关联。
- 继续练习进入主要会话草稿，保留原草稿，不自动发送。
- 关联随档案迁移；不可用的会话提示重新关联，不影响学习证据。
- 关联不读取聊天内容，也不自动合并学习记录。
- 新增会话关联、认证及前端桥接测试。
